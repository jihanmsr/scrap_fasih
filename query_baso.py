import pandas as pd
import sqlite3

# wait, we don't have sqlite3, we have been using BigQuery/SQL Lab before?
# No, earlier we queried a database or was it a CSV?
# Wait! In previous steps, the user was copying my SQL queries and running them in their own SQL Lab (a browser interface). I don't have direct database access! I just provide the SQL to them.
